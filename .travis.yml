language: python
python:
  - "2.7"
# command to install dependencies
install: "pip install numpy; pip install scipy; pip install scikit-image; pip install -r requirements.txt"
# command to run tests
script: nosetests